---
"eslint-plugin-yak": patch
---

style-conditions: show a concrete before/after built from your own code instead of a generic hint, split the "css literal used as a declaration value" case into move-property vs drop-css, set requiresTypeChecking to false (the rule is pure AST), and fix the docs url that 404'd on every violation
