---
"yak-swc": minor
"next-yak": minor
---

Support the new `@yak/solid` package: the compiler now detects which yak package a file imports from. Custom bundler integrations can import the cross-file resolver from `next-yak/cross-file-resolver` and the `.yak` file evaluator from `next-yak/isolated-source-eval`.
