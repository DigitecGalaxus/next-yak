---
"eslint-plugin-yak": minor
---

Add the `precompute-style-prop-values` rule, which reports prop values that JSX folding evaluates more than once and asks for them to be computed into a variable first
