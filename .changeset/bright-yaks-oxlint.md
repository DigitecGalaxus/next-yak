---
"eslint-plugin-yak": minor
---

Add direct Oxlint JS plugin support while preserving ESLint compatibility. Rules now use Oxlint's `createOnce` API and can be loaded directly through `jsPlugins` without installing ESLint.
