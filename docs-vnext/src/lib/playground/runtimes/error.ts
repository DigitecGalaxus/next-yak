/** The preview's shadow root styles this class. */
export const ERROR_CLASS = "playground-error";

export const errorStyles = `
.${ERROR_CLASS} {
  margin: 16px;
  padding: 12px;
  border-radius: 8px;
  background: #fdecec;
  color: #8a1c1c;
  font: 13px/1.5 ui-monospace, monospace;
  white-space: pre-wrap;
}
`;
