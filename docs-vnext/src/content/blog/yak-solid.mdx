---
title: yak for Solid
description: The Solid runtime, what it shares with React, and where the two differ.
date: 2026-09-15
author: The yak team
---

<Callout title="Draft">
  This post is not written yet. The outline below is what it will cover.
</Callout>

## What this post will cover

- What `@yak/solid` is, and which Solid version it needs.
- The one compiler, the two runtimes: the SWC plugin extracts the same CSS for both.
- What reactive props mean for a styled component, and why Solid needs its own runtime.
- Server rendering, and what the e2e suite checks on every commit.
- What is not supported yet.

## Trying it today

Install `@yak/solid` and read the [Vite guide](/docs/vite). The Solid setup uses
the same plugin.
